# @package graph_generation
type: "klein_bottle"
klein_bottle:
  u_points: 30  # Discretization in u parameter direction [0, 2π]
  v_points: 30  # Discretization in v parameter direction [0, 2π]
output:
  file_path: "temp/graph.pkl"
  visualize: true
  print_stats: true
